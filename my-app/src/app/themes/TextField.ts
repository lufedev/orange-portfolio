'use client'
import { createTheme } from '@mui/material/styles'

export const TextFieldTheme = createTheme({
  palette: {
    primary: {
      main: '#2348B1',
      contrastText: '#000000'
    }
  }
})
