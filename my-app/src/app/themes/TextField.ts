'use client'
import { createTheme } from '@mui/material/styles'

export const TextFieldTheme = createTheme({
  palette: {
    primary: {
      main: '#2348B1'
    },
    error: {
      main: '#DD0000'
    }
  }
})
